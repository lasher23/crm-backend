# CRM Backend

A simple REST API for managing customers. Built with Go and Gorilla Mux.

**Features:**
- Get all customers or a single one by ID
- Add, update, and delete customers
- In-memory storage (no database needed)
- Frontend with nice UI and API Client mode

**Dependencies:**
- For Method based HTTP routing: https://github.com/gorilla/mux
- For UUID generation: https://github.com/google/uuid
## Getting started

Install dependencies:

```bash
go mod download
```

Run the server:

```bash
go run .
```

Server starts on `http://localhost:3003`. Check `examples.http` for ready-to-use requests or just open open http://localhost:3003 

After opening the UI make sure to switch between UI and API mode in the top right corner as you prefer.

## Key features

* Getting all customer via GET /customers
  * Lists all customers with id, name, role, e-mail, phone, contacted
* Get single customer via GET /customers/{id}
  * Returns 404 when not found
* Creating customer via POST /customers
  * Name is required
  * Id will be generated, not allowed to pass it
* Update a single user via PUT /customers/{id}
  * Returns 404 when not found
  * Returns 400 when name is set to empty
* Delete a single user via DELETE /customers/{id}
  * Returns 404 when not found
  * Returns 204 when successful

# AI Usage for static content

Since this project is not about creating websites the whole content in static is generated by AI with GitHub Copilot and Sonnet 4.6. The code that serves the static folder was not created by AI, since this should be actively learned.